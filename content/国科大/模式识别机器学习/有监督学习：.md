从有标记的数据中推断函数
- 产生式模型：
	- 根据条件概率密度来预测
	- 最大化后验概率求解GM（生成式模型）正则化的最小均方误差LMS与最大化后验概率MAP等价
	- 最大后验 等同于最小化带有正则项的平方和误差。
- 判别式模型
	- 寻找一个函数，将每个输入直接映射到目标输出
	- 最大似然估计【MLE（Max Likelihood Estimator）】求解DM（判别式模型）
	- 最大似然相当于最小化平方误差
- 回归任务：
	- 线形回归的目标：
		- 利用给定样本来学习函数$f(w,x) =w_0+w_1x,w \in \{w_0, w_1\}^T$
		- 